\paragraph{Adapting and reusing drafters.}
EDA separates the drafter into shared and target-specific components and trains the lightweight private component, whereas RelaySpec trains five maps at DFlash's hidden-state input and leaves the drafter transformer unchanged~\citep{lin2026eda}. Online Speculative Decoding updates drafters from observed serving queries and OmniDraft adds an online n-gram cache, hybrid distillation and vocabulary conversion, whereas RelaySpec trains offline with no serving-time training~\citep{liu2024online,ramakrishnan2025omnidraft}. PARD trains a target-independent parallel drafter for a family of targets and PARD-2 supports both modes, whereas RelaySpec starts from an already trained target-conditioned drafter and changes only its feature interface~\citep{an2026pard,an2026pard2}.

\paragraph{Adapting pretrained drafters through target features.}
SD$^2$ is the closest method-level comparison because it also conditions a pretrained drafter on target hidden states~\citep{berdoz2026steering}. Its main method injects a learned steering signal into every feed-forward layer and fully fine-tunes the drafter; even its frozen-drafter variant still injects inside every feed-forward layer, whereas RelaySpec places the maps before the existing fusion projection and folds them into it before inference. RepSpec adds redundant linear structure during draft training and merges it away at inference~\citep{huo2026repspec}; RelaySpec uses the same property in a different place, so the adapted drafter has exactly the inference cost of the original. Table~\ref{tab:related} summarizes where each method places its trainable parameters.

\begin{table}[t]
\centering
\caption{Where each method places trainable parameters when a target changes. \emph{Draft network frozen}: no weight inside the drafter's own transformer or prediction head is updated. \emph{No added inference cost}: the trained parameters are multiplied into an existing weight matrix beforehand, so the adapted model runs the same operations at the same speed as the original. \emph{Reuses a released drafter}: adaptation starts from a published checkpoint.}
\label{tab:related}
\setlength{\tabcolsep}{3pt}
\scriptsize
\begin{tabular}{llcccc}
\toprule
Method & What is trained & \shortstack{Uses target\\features} & \shortstack{Draft network\\frozen} & \shortstack{No added\\inference cost} & \shortstack{Reuses a\\released drafter} \\
\midrule
EAGLE-3~\citep{li2025eagle3} & Draft network & Yes & No & No & No \\
DFlash~\citep{chen2026dflash} & Draft transformer & Yes & No & No & No \\
RepSpec~\citep{huo2026repspec} & Draft weights & Yes & No & Yes & No \\
PARD-2~\citep{an2026pard2} & Parallel drafter & Optional & No & No & No \\
EDA~\citep{lin2026eda} & Private component & Yes & No & No & Partly \\
OmniDraft~\citep{ramakrishnan2025omnidraft} & Cache, distill. & No & No & No & Yes \\
SD$^2$~\citep{berdoz2026steering} & Steering, drafter & Yes & Frozen variant & No & Yes \\
\midrule
RelaySpec (ours) & Input maps only & Yes & Yes & Yes & Yes \\
\bottomrule
\end{tabular}
\end{table}
