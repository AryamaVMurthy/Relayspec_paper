RelaySpec trains one linear map per target feature stream, keeps the drafter frozen, and folds the maps into the existing fusion projection, so inference runs the original architecture with one updated matrix. It improves every fine-tuned adapter we tested at two model scales, and within a model family recovers essentially all of a purpose-built native drafter on mathematical reasoning with no drafter training, from as little as 16 examples.
