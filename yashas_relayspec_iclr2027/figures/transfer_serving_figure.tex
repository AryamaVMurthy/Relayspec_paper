\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/relayspec_transfer_serving.pdf}
    \caption{Qwen3-8B $\leftarrow$ Qwen3-4B cross-transfer on vLLM. Each point uses 128 prompts per workload. Solid lines show RelaySpec and dashed lines show the native drafter. (a) Server aggregate throughput in tokens/s. (b) Client-side per-request throughput in tokens/s, computed from returned tokens and client-side request latency.}
    \label{fig:transfer-serving}
\end{figure}
