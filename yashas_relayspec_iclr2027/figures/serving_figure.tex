\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/relayspec_serving.pdf}
    \caption{Three LoRA-fine-tuned Qwen3-4B targets sharing one drafter on vLLM. Each concurrency setting uses 384 requests. (a) Server aggregate throughput. (b) Mean client-side per-request throughput. Both are measured in tokens/s.}
    \label{fig:serving}
\end{figure}
