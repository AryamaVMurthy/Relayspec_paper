The \emph{original target} is the model used to train the available drafter. The \emph{new target} is the model we want to accelerate. For cross-transfer, the original/reference target supplies reference features for training the maps and we compare against the native drafter. For a LoRA-fine-tuned target, we compare with the unadapted drafter: the released checkpoint used without maps.
